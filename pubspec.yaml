name: waiting
description: "Waiting is a part of any app: loading resources takes time and the user needs to be informed that he has to wait. This package provides a `WaitingWidget`: a widget showing a progress indicator, including animations, transitions and customizations."
version: 1.0.3
repository: https://github.com/tusche/waiting.git

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  async: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
